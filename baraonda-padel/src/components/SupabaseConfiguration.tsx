export function SupabaseConfiguration() { return <main className="configuration-page"><h1>Supabase non è configurato</h1><p>Per usare la sincronizzazione multi-dispositivo, crea un progetto Supabase e configura le variabili ambiente.</p><ol><li>Copia <code>.env.example</code> in <code>.env.local</code>.</li><li>Inserisci <code>VITE_SUPABASE_URL</code> e <code>VITE_SUPABASE_PUBLISHABLE_KEY</code>.</li><li>Applica le migrazioni con <code>npm run supabase:push</code>.</li><li>Riavvia l’app.</li></ol><p>Per provare l’app senza rete, imposta <code>VITE_DATA_PROVIDER=local</code> in <code>.env.local</code>.</p></main>; }
